# -*- mode: eshell -*-
# Eshell rc script - runs for each new shell

# Aliases managed by iota-shell.el:
# - ls -> eza (with git integration)

# Additional aliases
alias ll "ls -l"
alias la "ls -la"
alias tree "eza --tree"

